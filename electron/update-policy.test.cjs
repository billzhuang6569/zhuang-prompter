/* eslint-disable @typescript-eslint/no-require-imports */
const {test}=require('node:test'); const assert=require('node:assert/strict');const {newer,safeUrl,selectArtifact}=require('./update-policy.cjs');
test('version comparison refuses downgrade and malformed release',()=>{assert(newer('0.1.10','0.1.4'));assert(!newer('0.1.4','0.1.4'));assert(!newer('0.1.3','0.1.4'));assert(!newer('v0.1.5','0.1.4'));});
test('only immutable trusted HTTPS release URLs',()=>{for(const u of ['https://evil.test/prompter/downloads/a','http://bill-api.whatonearth.work/prompter/downloads/a','https://bill-api.whatonearth.work/skillhub/a','https://x:secret@bill-api.whatonearth.work/prompter/downloads/a'])assert.throws(()=>safeUrl(u));});
test('release must match platform, architecture and integrity',()=>{const m={schemaVersion:1,product:'zhuang-prompter',version:'0.1.5',artifacts:[{platform:'macos',arch:'arm64',format:'dmg',url:'https://bill-api.whatonearth.work/prompter/downloads/0.1.5/test.dmg',sha256:'a'.repeat(64),bytes:100}]};assert(selectArtifact(m,'macos','arm64','0.1.4'));assert.throws(()=>selectArtifact(m,'windows','x64','0.1.4'));assert.equal(selectArtifact(m,'macos','arm64','0.1.5'),null);});
